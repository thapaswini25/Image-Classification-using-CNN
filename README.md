# Image-Classification-using-CNN
This project builds a Convolutional Neural Network (CNN) to classify images such as cats and dogs. The model learns visual patterns from training data and predicts image categories. Implemented using Python, TensorFlow/Keras with accuracy visualization.
